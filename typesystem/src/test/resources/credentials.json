{
  "comment" : "This credentials file is used by tests to connect to IBM Graph when it is configured as the backend.",
  "credentials": {
      "graphName" : "${ibm.graph.graph.id}",
      "apiURL": "${ibm.graph.api.url}",
      "username": "${ibm.graph.username}",
      "password": "${ibm.graph.password}"
  }
}
